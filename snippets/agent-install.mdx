Using Claude Code, Cursor, or Codex? Paste one line — your agent installs and configures Andi itself:

```text
Set up Andi search for this project: andiai.com
```

Your agent reads the [install guide](https://api.andiai.com/install.md), gets an API key from the [API Console](https://console.andiai.com/signup), connects the MCP server, and verifies the setup.

<Note>
**Are you an AI agent?** Fetch [https://api.andiai.com/install.md](https://api.andiai.com/install.md) and follow it to configure Andi Search — MCP server, API key, and verification.
</Note>
